// useReputationScore — reads a wallet's score from ReputationRegistry.
// TODO: use wagmi `useReadContract` to call scoreOf(address).
// Returns: { score: number | undefined, isLoading: boolean, error: Error | null }
