// useToxicEvents — streams ToxicTradeReported events in real time.
// TODO: use viem `watchContractEvent` or wagmi `useWatchContractEvent`
//       to subscribe to GlyphHook's ToxicTradeReported events.
// Returns: { events: ToxicTradeEvent[], isLoading: boolean }
