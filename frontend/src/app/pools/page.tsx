// /pools — Pool list and swap interface.
// TODO: list active Glyph pools (Pool A + Pool B from Day 4 deploy).
// Each pool card shows: token pair, current fee tier, hook address.
// CTA: "Swap" button → opens swap modal.
