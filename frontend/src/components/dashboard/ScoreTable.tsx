// ScoreTable — live table of wallet reputation scores.
// TODO: subscribe to ScoreUpdated events and render rows per wallet.
// Columns: Wallet | Score (0-10000) | Last Updated | Risk Level (colour-coded)
